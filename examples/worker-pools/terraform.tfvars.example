# Copy this file to terraform.tfvars and fill in your values.

# Fully-qualified domain for n8n. The parent zone must be hosted in Route53.
n8n_domain = "n8n.example.com"

# Route53 hosted zone ID for the parent domain (e.g. example.com). The module
# issues the ACM certificate, validates it, and creates the alias A-record
# here: single terraform apply, no manual DNS steps.
route53_zone_id = "Z0123456789ABCDEFGHIJ"

# n8n Enterprise license activation key. Get one at https://n8n.io/pricing.
# Worker pools need the feat:workerPools entitlement on top of the usual ones.
n8n_license_key = "your-enterprise-license-key-here"

# Helm chart that renders queueMode.workerGroups. REQUIRED by this example:
# the module's default chart predates the feature and would silently render no
# pools. At the time of writing no published chart version carries it (it is
# n8n-io/n8n-hosting#189), so this is a preview build you packaged yourself and
# pushed to a registry you control; see README.md, "Getting a chart that
# renders pools". Once the feature is released, pin that version here and drop
# n8n_chart_repository to fall back to the public upstream registry.
n8n_chart_version    = "1.11.0-preview.workerpools.1"
n8n_chart_repository = "oci://123456789012.dkr.ecr.us-east-1.amazonaws.com/n8n-helm-chart"

# First n8n release that reads N8N_WORKER_POOLS_ENABLED and N8N_WORKER_POOL_NAME
# is 2.39.0. The chart's floating `stable` tag may still be older, and an older
# image ignores both variables without complaint, so pin explicitly.
n8n_image_tag = "2.39.0"

# AWS region. Change if you're deploying outside us-east-1.
# aws_region = "eu-west-1"

# EKS cluster name. Keep to 14 characters or fewer, because ElastiCache cluster IDs
# derived from this name are capped at 20 chars, and '-redis' consumes 6.
# cluster_name = "n8n-cluster"

# Extra AWS tags applied to every resource this example creates.
# tags = {
#   Environment = "production"
#   Owner       = "platform-team"
# }

# Extra hostnames n8n should answer on, beyond n8n_domain. Each one is added to
# the module-issued certificate as a subject alternative name, gets its own
# Route 53 validation and alias records, and is routed by the Ingress. n8n_domain
# stays canonical: it is what n8n advertises as its webhook URL.
# n8n_additional_domains = ["hooks.n8n.example.com"]

# Where n8n stores execution data. Default "database" keeps it in PostgreSQL.
# "s3" offloads every new execution to the bucket the module already creates for
# binary data. This example runs the module's default db.t3.small on 50 GB of gp2
# with a 150 IOPS baseline, the least room of any sizing this module ships, so
# reach for this before resizing the database. It needs n8n >= 2.27 and an
# Enterprise license with the feat:executionDataS3 entitlement, and it does not
# backfill: existing executions stay readable where they were written. Read the
# execution data section of the root README first, including the durability
# trade-off (the bucket has no backups and no versioning) and the S3 lifecycle
# constraint.
# n8n_execution_data_storage_mode = "s3"

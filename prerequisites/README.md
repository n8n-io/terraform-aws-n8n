# n8n on EKS — Prerequisites

Foundation workspace for the root [`terraform-aws-n8n`](../) module. Holds the resources whose lifecycle is slower than the application stack:

- VPC (public + private subnets, NAT gateway, EKS/ALB subnet tags)
- ACM certificate and its DNS validation waiter

The root module reads this workspace's outputs via `terraform_remote_state` — domain, region, and cluster name are not re-declared there.

For apply and teardown steps, see the [prerequisites guide](../docs/prerequisites.md).

## Why separate?

- **Isolates the one manual DNS step.** The registrar touch happens here; the root module applies unattended.
- **Fast app-stack iteration.** Re-applying n8n config doesn't touch the VPC or re-validate the cert.
- **HCP Terraform ready.** Each workspace maps 1:1 to a future HCP Terraform workspace.

## Outputs consumed by the root module

| Output | Purpose |
|---|---|
| `aws_region` | AWS provider configuration |
| `cluster_name` | EKS cluster name, subnet tags, IAM role naming |
| `n8n_domain` | n8n env vars, Ingress host rule |
| `vpc_id` | LBC, RDS/Redis security groups |
| `private_subnets` | Nodes, RDS subnet group, Redis subnet group |
| `public_subnets` | ALB placement |
| `vpc_cidr_block` | RDS/Redis ingress rules |
| `certificate_arn` | ALB Ingress TLS termination |

## Reference

<!-- The block below is auto-generated by terraform-docs. Run `terraform-docs markdown table --output-file README.md --output-mode inject .` to refresh it. -->

<!-- BEGIN_TF_DOCS -->
## Requirements

| Name | Version |
| ---- | ------- |
| <a name="requirement_terraform"></a> [terraform](#requirement\_terraform) | >= 1.7 |
| <a name="requirement_aws"></a> [aws](#requirement\_aws) | ~> 5.0 |

## Providers

| Name | Version |
| ---- | ------- |
| <a name="provider_aws"></a> [aws](#provider\_aws) | ~> 5.0 |

## Modules

| Name | Source | Version |
| ---- | ------ | ------- |
| <a name="module_vpc"></a> [vpc](#module\_vpc) | terraform-aws-modules/vpc/aws | ~> 5.0 |

## Resources

| Name | Type |
| ---- | ---- |
| [aws_acm_certificate.n8n](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/acm_certificate) | resource |
| [aws_acm_certificate_validation.n8n](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/acm_certificate_validation) | resource |
| [aws_availability_zones.available](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/data-sources/availability_zones) | data source |

## Inputs

| Name | Description | Type | Default | Required |
| ---- | ----------- | ---- | ------- | :------: |
| <a name="input_aws_region"></a> [aws\_region](#input\_aws\_region) | AWS region to deploy into (e.g. us-east-1, eu-west-1, ap-southeast-1). Must match the region the root module is applied in. | `string` | `"us-east-1"` | no |
| <a name="input_cluster_name"></a> [cluster\_name](#input\_cluster\_name) | Name for the EKS cluster that will consume this VPC. Keep to 14 characters or fewer so the root module (which derives an ElastiCache cluster ID of `<cluster_name>-redis`, capped at 20 chars) stays within AWS limits. | `string` | `"n8n-cluster"` | no |
| <a name="input_n8n_domain"></a> [n8n\_domain](#input\_n8n\_domain) | Fully-qualified domain name for n8n (e.g. n8n.example.com). You must own this domain and be able to edit its DNS records — the ACM certificate is DNS-validated. | `string` | n/a | yes |
| <a name="input_tags"></a> [tags](#input\_tags) | Additional AWS tags to apply to all resources created by this workspace. Merged on top of the built-in ManagedBy/Project tags. | `map(string)` | `{}` | no |

## Outputs

| Name | Description |
| ---- | ----------- |
| <a name="output_acm_validation_cname_name"></a> [acm\_validation\_cname\_name](#output\_acm\_validation\_cname\_name) | CNAME record NAME to add at your DNS provider (certificate validation) |
| <a name="output_acm_validation_cname_value"></a> [acm\_validation\_cname\_value](#output\_acm\_validation\_cname\_value) | CNAME record VALUE to add at your DNS provider (certificate validation) |
| <a name="output_aws_region"></a> [aws\_region](#output\_aws\_region) | AWS region the VPC and certificate were created in |
| <a name="output_certificate_arn"></a> [certificate\_arn](#output\_certificate\_arn) | ARN of the validated ACM certificate — wired into the ALB Ingress by the root module |
| <a name="output_cluster_name"></a> [cluster\_name](#output\_cluster\_name) | EKS cluster name the root module must use (subnet tags reference this) |
| <a name="output_n8n_domain"></a> [n8n\_domain](#output\_n8n\_domain) | Domain name the ACM certificate was issued for |
| <a name="output_private_subnets"></a> [private\_subnets](#output\_private\_subnets) | Private subnet IDs — nodes, RDS, and ElastiCache attach here |
| <a name="output_public_subnets"></a> [public\_subnets](#output\_public\_subnets) | Public subnet IDs — the ALB attaches here |
| <a name="output_vpc_cidr_block"></a> [vpc\_cidr\_block](#output\_vpc\_cidr\_block) | VPC CIDR block — used by RDS and Redis security groups to allow intra-VPC traffic |
| <a name="output_vpc_id"></a> [vpc\_id](#output\_vpc\_id) | VPC ID — used by the AWS Load Balancer Controller and security groups in the root module |
<!-- END_TF_DOCS -->
